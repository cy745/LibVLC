---
phase: 02
plan: 02
type: execute
wave: 2
depends_on:
  - 02-01
files_modified:
  - .github/workflows/build-libvlc.yml
  - README.md
autonomous: true
requirements:
  - MAC-02
  - MAC-03
user_setup: []

must_haves:
  truths:
    - "GitHub Action triggers on push and builds macOS successfully"
    - "macOS libvlc.dylib artifact is uploaded"
    - "README documents macOS build steps and artifacts"
  artifacts:
    - path: ".github/workflows/build-libvlc.yml"
      provides: "GitHub Actions workflow with macOS matrix"
      min_lines: 50
    - path: "README.md"
      provides: "Updated README with macOS documentation"
  key_links:
    - from: ".github/workflows/build-libvlc.yml"
      to: "build-libvlc-macos.sh"
      via: "run step"
      pattern: "build-libvlc-macos.sh"
---

<objective>
Extend GitHub Actions workflow with macOS build matrix and update README with macOS documentation. Plan 01 created the macOS build script; this plan wires it into CI and documents usage.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.github/workflows/build-libvlc.yml
@build-libvlc.sh
@build-libvlc-macos.sh
@README.md
@docs/build-libvlc.md
</context>

<interfaces>
<!-- Key patterns from existing workflow -->

Windows Build Job Pattern:
```yaml
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Build VLC
        run: docker run --rm ... ./extras/package/win32/build.sh
      - uses: actions/upload-artifact@v4
        with:
          name: libvlc-windows-${{ env.VLC_COMMIT }}
```

macOS Build Requirements:
- runs-on: macos-latest (D-03)
- No Docker - native build
- Uses build-libvlc-macos.sh
- Uploads libvlc.dylib and modules
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Extend GitHub Actions workflow with macOS matrix</name>
  <files>.github/workflows/build-libvlc.yml</files>
  <read_first>
    - .github/workflows/build-libvlc.yml
  </read_first>
  <acceptance_criteria>
    - grep -c "os: \[windows, macos\]" .github/workflows/build-libvlc.yml returns 1
    - grep -c "runs-on: macos-latest" .github/workflows/build-libvlc.yml returns 1
    - grep -c "build-libvlc-macos.sh" .github/workflows/build-libvlc.yml returns 1
    - grep -c "libvlc-macos-" .github/workflows/build-libvlc.yml returns 1
    - grep -c "output-macos" .github/workflows/build-libvlc.yml returns 1
  </acceptance_criteria>
  <action>
Extend .github/workflows/build-libvlc.yml to add macOS to the matrix. The workflow should:

1. Add `os: [windows, macos]` to the strategy matrix
2. Add conditional `runs-on` based on os type:
   - windows: ubuntu-latest with Docker
   - macos: macos-latest (native)
3. Add a separate build step for macOS that:
   - Checks out code
   - Runs ./build-libvlc-macos.sh
   - Uploads output-macos directory as artifact with name `libvlc-macos-${{ env.VLC_COMMIT }}`

The key changes to the workflow file:

After `jobs:`, add strategy matrix. Change the jobs structure to be:

```yaml
jobs:
  build:
    strategy:
      matrix:
        os: [windows, macos]
        include:
          - os: windows
            runs-on: ubuntu-latest
          - os: macos
            runs-on: macos-latest

    runs-on: ${{ matrix.runs-on }}

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      # Windows build steps (existing, but use matrix.os == 'windows' condition)
      - name: Check VLC commit (windows)
        if: matrix.os == 'windows'
        run: |
          echo "VLC Commit: ${{ env.VLC_COMMIT }}"
          ...

      # macOS build steps (NEW)
      - name: Build VLC for macOS
        if: matrix.os == 'macos'
        run: |
          ./build-libvlc-macos.sh

      - name: Upload macOS artifacts
        if: matrix.os == 'macos'
        uses: actions/upload-artifact@v4
        with:
          name: libvlc-macos-${{ env.VLC_COMMIT }}
          path: |
            output-macos/*.dylib
            output-macos/modules/
          retention-days: 30
```

The existing Docker-based Windows build steps should be kept but wrapped with `if: matrix.os == 'windows'` condition.
  </action>
  <verify>
    <automated>grep -q "os: \[windows, macos\]" .github/workflows/build-libvlc.yml && grep -q "runs-on: macos-latest" .github/workflows/build-libvlc.yml && grep -q "build-libvlc-macos.sh" .github/workflows/build-libvlc.yml</automated>
  </verify>
  <done>.github/workflows/build-libvlc.yml has matrix strategy with os: [windows, macos], macos-latest runner, and build-libvlc-macos.sh invocation</done>
</task>

<task type="auto">
  <name>Task 2: Update README.md with macOS documentation</name>
  <files>README.md</files>
  <read_first>
    - README.md
    - docs/build-libvlc.md
  </read_first>
  <acceptance_criteria>
    - grep -c "macOS" README.md returns 1 or more
    - grep -c "build-libvlc-macos.sh" README.md returns 1
    - grep -c "Universal Binary" README.md returns 1
    - grep -c "libvlc.dylib" README.md returns 1
  </acceptance_criteria>
  <action>
Update README.md to add macOS section. Add after the existing "## 快速开始" section (or similar Chinese section structure), a new macOS section.

The macOS section should include:

```markdown
## macOS 构建

为 macOS x86_64 + arm64 (Universal Binary) 构建 libvlc.dylib。

### 环境要求

| 软件 | 版本 | 说明 |
|------|------|------|
| macOS | 13+ (Ventura) | 原生构建环境 |
| Xcode | Latest | 编译器工具链 |
| Git | Latest | 源码管理 |

### 构建步骤

```bash
# 设置执行权限
chmod +x build-libvlc-macos.sh

# 执行构建 (需要 macOS 环境)
./build-libvlc-macos.sh
```

### 构建产物

| 文件 | 说明 |
|------|------|
| `libvlc.dylib` | libVLC 主库 (Universal Binary, arm64 + x86_64) |
| `modules/*.dylib` | 插件目录 |

### GitHub Actions

macOS 构建通过 GitHub Actions 自动执行。使用 `macos-latest` runner 原生构建。

### 跨平台使用

```bash
# 编译链接
clang -o myapp myapp.c \
    -lvlc -L./output-macos -I./include
```

注意: macOS 版 libvlc.dylib 是 Universal Binary，可同时在 Intel 和 Apple Silicon Mac 上运行。
```

Also update the directory structure at the top of README.md to include macOS artifacts:

Update the directory structure section to show:
```
LibVLC/
├── build-libvlc.sh           # Windows 构建脚本
├── build-libvlc-macos.sh     # macOS 构建脚本 (NEW)
├── output/                    # Windows 构建产物
├── output-macos/              # macOS 构建产物 (NEW)
...
```
  </action>
  <verify>
    <automated>grep -q "macOS" README.md && grep -q "build-libvlc-macos.sh" README.md && grep -q "libvlc.dylib" README.md</automated>
  </verify>
  <done>README.md updated with macOS section, build instructions, and output-macos directory</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| GitHub Runner -> VLC Repo | Untrusted network input (git clone) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-03 | Tampering | VLC source code | mitigate | Verify VLC_COMMIT matches known-good SHA pair from Phase 1 |
| T-02-04 | Information | Artifact names | accept | Commit SHA in artifact name is not sensitive |
</threat_model>

<verification>
1. Verify workflow file has matrix with os: [windows, macos]
2. Verify macos-latest runner is specified
3. Verify build-libvlc-macos.sh is called in workflow
4. Verify macOS artifact upload step exists
5. Verify README has macOS section
</verification>

<success_criteria>
- .github/workflows/build-libvlc.yml extended with macOS matrix
- macOS job uses runs-on: macos-latest
- build-libvlc-macos.sh is invoked in macOS job
- libvlc-macos- artifact is uploaded
- README.md has macOS build documentation
</success_criteria>

<output>
After completion, create `.planning/phases/02-add-macos-build-support/02-02-SUMMARY.md`
</output>
